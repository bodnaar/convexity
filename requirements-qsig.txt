# Extra dependencies for the qsig experiment layer (DGMM 2027).
# The published requirements.txt pins the environment the DGCI 2019 / IWCIA 2025
# results were produced in; install it first, in a venv, so the execution server
# and this machine agree:
#     python -m venv .venv && . .venv/bin/activate
#     pip install -r requirements.txt -r requirements-qsig.txt
pillow>=10.0
pytest>=7.0
